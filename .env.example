_ENV=development
CORS_ORIGIN=http://localhost:3000

# Database (replace user:password with your local Postgres creds)
DATABASE_URL="postgresql://user:password@localhost:5432/averulo"

# Auth
JWT_SECRET=dev-secret

# Paystack
PAYSTACK_SECRET_KEY=sk_test_xxxxx
PAYSTACK_PUBLIC_KEY=pk_test_xxxxx
PAYSTACK_CALLBACK_URL=http://localhost:3000/payments/callback

# SMTP (dev mode will return OTP in API response)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass
EMAIL_FROM="Averulo <no-reply@averulo.local>"
